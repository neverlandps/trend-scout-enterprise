name: Backend Tests and Docker Build

on:
  push:
    branches: [main, develop]
    tags: ['v*']
  pull_request:
    branches: [main]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/trend-scout-enterprise-backend
  # GitHub runners are outside China; pypi.org is the nearest fast index.
  # The Tencent mirror stays as extra-index-url fallback in pip.conf (test job)
  # and as a build-arg option for the Docker build.
  PIP_INDEX_URL: https://pypi.org/simple/
  PIP_TRUSTED_HOST: mirrors.cloud.tencent.com

jobs:
  test-backend:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    services:
      postgres:
        image: postgres:16
        env:
          POSTGRES_USER: trend_scout
          POSTGRES_PASSWORD: trend_scout
          POSTGRES_DB: trend_scout
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U trend_scout -d trend_scout"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
    env:
      DATABASE_URL: postgresql+psycopg2://trend_scout:trend_scout@localhost:5432/trend_scout
      TEST_DATABASE_URL: postgresql+psycopg2://trend_scout:trend_scout@localhost:5432/trend_scout
      SECRET_KEY: ci-test-secret
      ENCRYPTION_SALT: dGVzdC1zYWx0LTEyMzQ1Ng==
      TESTING: "1"
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Configure pip to use Tencent mirror
        run: |
          mkdir -p ~/.config/pip
          printf '[global]\nindex-url = https://pypi.org/simple/\nextra-index-url = https://mirrors.cloud.tencent.com/pypi/simple/\ntrusted-host = mirrors.cloud.tencent.com\n' > ~/.config/pip/pip.conf
          python -m pip install --upgrade pip

      - name: Install dependencies
        run: |
          set -o pipefail
          pip install --no-cache-dir --retries 10 --timeout 120 -e ".[dev]" 2>&1 | tee /tmp/pip-install.log

      - name: Post install log to PR on failure
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.readFileSync('/tmp/pip-install.log', 'utf8');
            const tail = log.split('\n').slice(-80).join('\n');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: '## Backend install failed — pip log tail\n\n```\n' + tail + '\n```'
            });

      - name: Run backend tests
        run: |
          set -o pipefail
          python -m pytest tests/ -v 2>&1 | tee /tmp/pytest.log

      - name: Post test log to PR on failure
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.readFileSync('/tmp/pytest.log', 'utf8');
            const tail = log.split('\n').slice(-150).join('\n');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: '## Backend tests failed — pytest log tail\n\n```\n' + tail + '\n```'
            });

      - name: Run backend smoke test
        run: |
          set -o pipefail
          python -m uvicorn trend_scout_enterprise.main:app --host 127.0.0.1 --port 8000 &
          sleep 3
          python scripts/smoke_test.py 2>&1 | tee /tmp/smoke.log
        # Smoke test exits 0 even if Redis is unavailable; it validates the
        # API surface, workspace routing, and trends aggregation.

      - name: Post smoke log to PR on failure
        if: failure() && github.event_name == 'pull_request'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.readFileSync('/tmp/smoke.log', 'utf8');
            const tail = log.split('\n').slice(-80).join('\n');
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.payload.pull_request.number,
              body: '## Backend smoke failed — log tail\n\n```\n' + tail + '\n```'
            });

  build-and-push:
    needs: test-backend
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=ref,event=branch
            type=semver,pattern={{version}}
            type=sha,prefix=,suffix=,format=short

      - name: Build and push backend image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: deployment/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
          provenance: false
          platforms: linux/amd64
          build-args: |
            PIP_INDEX_URL=https://mirrors.cloud.tencent.com/pypi/simple/
            PIP_TRUSTED_HOST=mirrors.cloud.tencent.com
